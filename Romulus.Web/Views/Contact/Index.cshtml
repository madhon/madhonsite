@model ContactViewModel

@{
    ViewBag.Title = "Contact";
    HtmlHelper.ClientValidationEnabled = true;
    HtmlHelper.UnobtrusiveJavaScriptEnabled = true;
}

<h2>Contact</h2>
@using (Html.BeginForm("Index", "Contact", FormMethod.Post, new { @name = "frmContact", @id = "frmContact", @class = "form-horizontal", @role = "form"  })  )
{   
    @Html.ValidationSummary(true)
    @Html.AntiForgeryToken()

    <fieldset>
        <legend>Your Information</legend>
        <div class="form-group">
            @Html.LabelFor(f => f.Name, new { @class = "col-md-2 control-label"})
            <div class="col-md-6">
                @Html.Html5TextBoxFor(f => f.Name, InputTypes.InputType.Text, new { @class ="form-control"}, "Your Name", false, false, true)
                @Html.ValidationMessageFor(f => f.Name, null, new { @class = "help-inline" })
            </div>
        </div>
        
        <div class="form-group">
            @Html.LabelFor(f => f.Email, new { @class = "col-md-2 control-label"})
            <div class="col-md-6">
                @Html.Html5TextBoxFor(f => f.Email, InputTypes.InputType.Email, new { @class ="form-control"}, "Your Email Address")
                @Html.ValidationMessageFor(f => f.Email, null, new { @class = "help-inline" })
            </div>
        </div>

        <div class="form-group">
            @Html.LabelFor(f => f.Message, new { @class = "col-md-2 control-label"})
            <div class="col-md-6">
                @Html.TextAreaFor(f => f.Message, 5, 100, new { @placeholder = "Your message", @class ="form-control"})
                @Html.ValidationMessageFor(f => f.Message, null, new { @class = "help-inline" })
            </div>
        </div>      
    </fieldset>
    
    <div class="form-actions">
     <button class="btn btn-primary" type="submit">Send Message</button>
     <button class="btn btn-default" type="reset">Cancel</button>
    </div>
}

@section scriptFiles {
    <script type="text/javascript" src="//ajax.aspnetcdn.com/ajax/jquery.validate/1.11.1/jquery.validate.min.js"></script>
    <script type="text/javascript" src="//ajax.aspnetcdn.com/ajax/mvc/3.0/jquery.validate.unobtrusive.min.js"></script>
    <script type="text/javascript">
        jQuery.validator.setDefaults({
            highlight: function (element, errorClass, validClass) {
                if (element.type === 'radio') {
                    this.findByName(element.name).addClass(errorClass).removeClass(validClass);
                } else {
                    $(element).addClass(errorClass).removeClass(validClass);
                    $(element).closest('.form-group').removeClass('has-success').addClass('has-error');
                }
            },
            unhighlight: function (element, errorClass, validClass) {
                if (element.type === 'radio') {
                    this.findByName(element.name).removeClass(errorClass).addClass(validClass);
                } else {
                    $(element).removeClass(errorClass).addClass(validClass);
                    $(element).closest('.form-group').removeClass('has-error').addClass('has-success');
                }
            }
        });
    </script>
}




